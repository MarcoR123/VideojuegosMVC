<div id="<%= dom_id category %>">
  <%= category.name %>
  <%= link_to t('.editar'), edit_category_path(category) %>
  <%= link_to t('.eliminar'), category_path(category), data: {
    turbo_method: :delete,
    turbo_confirm: '¿Estás seguro?'
  } %>
</div>